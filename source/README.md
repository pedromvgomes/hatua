# source

The Hatua monorepo — the React workflow designer and everything it ships with — lives here.

Empty for now; the workspace lands in a follow-up. Keeping it under `source/` leaves room at the
repo root for things that are *about* Hatua without being *part of* it — starting with
[`../brand/`](../brand/), the logo and its PNG tooling.

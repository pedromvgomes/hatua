# primitives

No domain knowledge. Button, Input, Select, Toggle, Toast, ConfirmDialog, Icon.

**Rule:** may not import any `@hatua/*` package or any higher tier. Enforced by
`noRestrictedImports` in the workspace `biome.json`.
